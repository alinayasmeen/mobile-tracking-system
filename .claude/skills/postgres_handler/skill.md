
# **Name:** `postgres_handler`

Allows the agent to create database schemas and run structured queries.

## Capabilities

* Generate SQL queries (DDL, DML)
* Create tables, indexes, constraints
* Write joins, aggregations, functions
* Provide SQLModel/SQLAlchemy ORM code
* Optimize queries for performance
* Integrate Postgres with FastAPI
